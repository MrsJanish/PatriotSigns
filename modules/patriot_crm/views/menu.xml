<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- 
        CRM Menu Overrides
        
        Most menus now come from patriot_base/views/menu_unified.xml
        This file only contains CRM-specific actions used by the unified menu.
    -->

    <!-- Action for Upcoming Bids (used by unified menu) -->
    <record id="action_upcoming_bids" model="ir.actions.act_window">
        <field name="name">Upcoming Bids</field>
        <field name="res_model">crm.lead</field>
        <field name="view_mode">list,kanban,form</field>
        <field name="domain">[('bid_date', '!=', False), ('bid_date', '&gt;=', context_today().strftime('%Y-%m-%d'))]</field>
        <field name="context">{'default_type': 'opportunity', 'search_default_groupby_bid_date': 1}</field>
    </record>

    <!-- 
        REMOVED: Duplicate menu items
        All menus now use the unified structure in patriot_base/views/menu_unified.xml
    -->

</odoo>
