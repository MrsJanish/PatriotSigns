<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <!-- 
            Record rules for time punches.
            Employees can only see/create their own punches.
            Managers can see all punches.
        -->
        
        <!-- Employees can only read their own punches -->
        <record id="rule_time_punch_employee_read" model="ir.rule">
            <field name="name">Time Punch: See Own Only</field>
            <field name="model_id" ref="model_ps_time_punch"/>
            <field name="domain_force">[('employee_id.user_id', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="False"/>
        </record>
        
        <!-- HR Managers can see all punches -->
        <record id="rule_time_punch_manager" model="ir.rule">
            <field name="name">Time Punch: Managers See All</field>
            <field name="model_id" ref="model_ps_time_punch"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('hr.group_hr_manager'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>
        
    </data>
</odoo>
