<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Time Clock Kiosk Form View - Standard Odoo styling -->
    <record id="ps_time_clock_kiosk_form" model="ir.ui.view">
        <field name="name">ps.time.clock.kiosk.form</field>
        <field name="model">ps.time.clock.kiosk</field>
        <field name="arch" type="xml">
            <form create="0" edit="1" delete="0">
                <sheet>
                    <div class="oe_title text-center">
                        <h1>Time Clock</h1>
                    </div>
                    
                    <!-- Hidden computed fields -->
                    <field name="is_clocked_in" invisible="1"/>
                    <field name="active_punch_id" invisible="1"/>
                    <field name="employee_id" invisible="1"/>
                    
                    <!-- Status Display -->
                    <div class="alert alert-success text-center" role="alert" 
                         invisible="not is_clocked_in" style="font-size: 16px;">
                        <strong>⏱️ Currently Working On:</strong>
                        <field name="current_project_id" readonly="1" class="fw-bold"/>
                        <br/>
                        <strong>Time:</strong>
                        <field name="current_duration" readonly="1" class="fw-bold"/>
                    </div>
                    
                    <div class="alert alert-secondary text-center" role="alert" 
                         invisible="is_clocked_in" style="font-size: 16px;">
                        <strong>⏸️ Not Clocked In</strong>
                    </div>
                    
                    <!-- Project Selection -->
                    <group>
                        <group string="Select Project" colspan="2">
                            <field name="project_id" 
                                   placeholder="Select a project..."
                                   options="{'no_create': True}"/>
                            <field name="task_id" 
                                   placeholder="(Optional) Select a task..."
                                   options="{'no_create': True}"/>
                            <field name="notes" 
                                   placeholder="What are you working on? (optional)"/>
                        </group>
                    </group>
                    
                    <!-- Action Buttons - centered -->
                    <div class="text-center mt-4 mb-4">
                        <button name="action_clock_in" 
                                string="🕐 Clock In" 
                                type="object"
                                class="btn btn-success btn-lg me-2"
                                style="font-size: 18px; padding: 12px 30px;"/>
                        
                        <button name="action_clock_out" 
                                string="🛑 Clock Out" 
                                type="object"
                                class="btn btn-danger btn-lg me-2"
                                invisible="not is_clocked_in"
                                style="font-size: 18px; padding: 12px 30px;"/>
                        
                        <button name="action_take_break" 
                                string="☕ Break" 
                                type="object"
                                class="btn btn-warning btn-lg"
                                invisible="not is_clocked_in"
                                style="font-size: 18px; padding: 12px 30px;"/>
                    </div>
                    
                </sheet>
            </form>
        </field>
    </record>

    <!-- Time Clock Kiosk Action -->
    <record id="ps_time_clock_kiosk_action" model="ir.actions.act_window">
        <field name="name">Time Clock</field>
        <field name="res_model">ps.time.clock.kiosk</field>
        <field name="view_mode">form</field>
        <field name="target">current</field>
        <field name="context">{'form_view_initial_mode': 'edit'}</field>
    </record>

</odoo>
